# ===============================
# Configuração do servidor
# ===============================
server:
  port: 8080

# ===============================
# Configuração do banco de dados MySQL
# ===============================
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/parfun_imports?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: A.marque@1979
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  sql:
    init:
      mode: always

  h2:
    console:
      enabled: false

# ===============================
# Configuração JWT
# ===============================
jwt:
  secret: umaChaveSecretaBemGrandeParaSeguranca123456
  expiration: 3600000

# ===============================
# Logs detalhados
# ===============================
logging:
  level:
    org.springframework: INFO
    com.parfunimports: DEBUG
    "[org.hibernate.SQL]": DEBUG
    "[org.hibernate.type.descriptor.sql.BasicBinder]": TRACE
